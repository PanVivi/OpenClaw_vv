# 001 OpenClaw架设部署｜RoleCardAudit 角色卡复审｜v0.02

## 一、复审范围

- 来源分支：`agent/record-housekeeper-deployment-status`
- 来源提交：`90cb37404f575a39d97230f3342e8c2afc597b24`
- 本地审核分支：`codex/audit-role-cards`
- 对象：来源分支全部 261 个文件，以及本轮产生的八套旧版归档、八套当前角色包和新版权威入口
- 原则：以现有八 Agent 设计为基础，只处理影响一致性、权限和快速上线的明确问题；不扩展 Agent 数量、服务或记忆范围

## 二、少主已确认的属性

1. `reviewer` 人格名称定为“夏姬”；固定 Agent ID、目录名和“合并审查”功能职责保持不变。
2. 獨孤伽羅、武曌、呂雉保持当前人物属性和互动边界，不继续扩写差异。
3. 賈南風与蕭觀音的较强羞辱性称呼维持默认可用，不增加场景开关；现有停止口令、严肃场景收敛和不得影响事实/权限/任务的规则继续有效。

## 三、本次复审直接修正

- reviewer v0.04 的展示人格统一为夏姬；`reviewer` ID、Review/Risk/Test 三阶段和目录保持稳定，避免部署迁移。
- ops、coder、reviewer 的 `SOUL.md`、`TOOLS.md`、`PERMISSIONS.md` 统一基础/增强口径：基础层使用当前正式会话内可核对、绑定材料且只使用一次的 Review/Risk 通过记录；专用持久化、目标 Generation 和硬 Gate 属于增强层。
- 修正 life `PERMISSIONS.md` 遗留的 v0.04 标识为当前 v0.05。
- 未改变八个 Agent 的职责归属、人格核心、权限方向、停止口令或部署阶段范围。

## 四、角色卡复审结论

| Agent | 当前候选版 | 文档结论 | 基础部署重点 |
| --- | --- | --- | --- |
| housekeeper / 賈南風 | v1.08 | 通过 | 稳定基线继承、直接回答、正确转交、无工程工具 |
| life / 蕭觀音 | v0.05 | 通过 | 生活设置唯一所有者、真实工具结果、工程能力拒绝 |
| ops / 魚玄機 | v0.06 | 通过 | 默认只读、一次性 Risk 通过记录、回滚与真实证据 |
| coder / 步非煙 | v0.06 | 通过 | 隔离实现、一次性 Review 通过记录、生产拒绝 |
| reviewer / 夏姬 | v0.04 | 通过 | 独立 Review/Risk/Test、只读审查、不修改待审对象 |
| companion-dugu / 獨孤伽羅 | v0.03 | 通过 | 独立会话、停止口令、工程权限拒绝 |
| companion-wu / 武曌 | v0.03 | 通过 | 独立会话、停止口令、工程权限拒绝 |
| companion-lv / 呂雉 | v0.03 | 通过 | 独立会话、停止口令、工程权限拒绝 |

设计层没有剩余的文档阻塞项。当前审核树可以进入固定提交和 Phase 1 受控候选部署，不应再为基础上线增加任务控制服务、完整记忆、硬 A2A 或技术子 Agent。

## 五、可部署性判定

- **角色设计：通过。** 八张当前角色卡可作为基础层候选源。
- **部署制品：条件通过。** 本轮修复仍在本地工作树，部署前必须形成固定 Git 提交，并以该提交生成五文件 SHA-256 清单；不能直接把尚未包含本轮修复的远端来源分支当作最终部署制品。
- **实际上线：待执行。** 尚未连接 NAS，workspace、模型、Bot/account/binding、真实 allow/deny、角色加载和负向测试未核验。
- **STABLE：暂不可标记。** 只有八 Agent 分别完成普通新会话、身份/称呼、允许能力、禁止能力、停止/取消和最小协作测试后，才能按各自状态升级。

因此，可以开始受控候选部署；不能声称已经部署，也不能直接升级为 `STABLE`。

## 六、远端分支核对

2026-07-23（Asia/Taipei）执行 `git fetch origin --prune` 后：

- 目标分支尖端仍为 `90cb37404f575a39d97230f3342e8c2afc597b24`，提交时间 2026-07-22 09:13:58 +08:00。
- 该提交时间晚于远端其余分支尖端，是当前仓库按最近提交时间计算的最新远端分支。
- 本地审核分支以该提交为精确基线，抓取后与目标远端为 `0/0` ahead/behind。
- 它不是所有远端分支的严格线性超集：`agent/align-workflow-fuse-rule` 有一个未合并的并行提交，只修改旧版 `Workflows工作流程-v0.05.md` 的熔断表述；当前权威賈南風 v1.08 已包含等价的自主熔断规则，因此不构成本次候选部署阻塞，也不回写旧版文档。

## 七、复审验证

- 全部文件可按严格 UTF-8 读取，无 NUL 文件。
- 八套角色包的必需文件齐全，当前版本入口一致。
- 八张 `AGENTS.md` 均包含共同协议 v0.02 的来源、可信程度、长期记忆限制、隐私和角色扮演损害禁止项。
- 当前权威入口的 Markdown 相对链接无断链。
- 部署状态枚举有效；`.env.example` 只有八个空 Bot token 变量，无已取消的三个 reviewer 阶段变量。
- 角色权限保持最小化：housekeeper/life/companions 无工程执行权，coder 无生产权，reviewer 无修改和生产权，ops 写入仍需受审授权。
- 八套旧候选版归档与来源提交对应文件保持一致。
- 未发现写入仓库的实际 secret；`git diff --check` 通过。
