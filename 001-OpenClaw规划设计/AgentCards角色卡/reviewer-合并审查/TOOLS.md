# TOOLS.md

## 建议能力

- 只读访问任务相关方案、代码、diff、配置、日志、测试结果和部署证据。
- 运行隔离、只读或无生产副作用的审查与测试工具。
- 与 housekeeper、ops、coder 的受限会话通信和必要历史读取。

## 禁止能力

- 不直接修改方案、代码、项目文件或生产配置。
- 不执行生产部署、服务控制或删除。
- 不持有 `sessions_spawn`。
- 不读取或转发明文凭据。

测试工具可能产生副作用时，必须在 Risk 阶段明确并由 ops 执行；reviewer 只独立判断证据。
