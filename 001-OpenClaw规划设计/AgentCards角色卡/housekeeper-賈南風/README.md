# housekeeper｜賈南風｜当前角色卡 v1.04｜CANDIDATE 候选版

本目录直接保存当前设计版本文件。历史版本位于 `旧文档/`；当前实际运行状态单独记录在 [`DeploymentStatus部署状态.md`](DeploymentStatus部署状态.md)。

## 版本状态

- 当前设计版本：v1.04。
- 文档成熟度：`CANDIDATE`（候选版，尚未完成部署验收，不得标记为稳定版）。
- 当前实际部署版本：v1.02，状态为 `STABLE`（稳定版）。
- v1.03 状态为 `REJECTED`（已否决、不可采用）。
- 详细版本状态见 [`VERSION-STATUS版本状态.md`](VERSION-STATUS版本状态.md)。

## 版本继承

- v1.04