# 步非煙版本状态

| 版本 | 状态 | 可否作为正式部署源 | 说明 |
| --- | --- | --- | --- |
| v0.01 | `REJECTED` | 否 | 隔离、测试、方案和交付不完整 |
| v0.02 | `REJECTED` | 否 | 缺少基线、持久化、硬白名单、依赖和脱敏 |
| v0.03 | `REJECTED` | 否 | 运行边界完整，但缺少任务所有者、Assignment Generation、接收确认、产物哈希交接和旧代次失效 |
| v0.04 | `CANDIDATE` | 暂否 | 已补齐统一工程交接契约，等待实际部署验收 |

当前没有 `STABLE` 版本。
