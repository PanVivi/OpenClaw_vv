# 步非煙版本状态

| 版本 | 状态 | 可否作为部署源 | 说明 |
| --- | --- | --- | --- |
| v0.01—v0.03 | `REJECTED` | 否 | 隔离、测试、交付、持久化或交接不完整 |
| v0.04 | `REJECTED` | 否 | 将正常 Generation 交接错误视为审查结论失效，且已消费 Gate 的返工语义不清 |
| v0.05 | `CANDIDATE` | 暂否 | 分离处理权、初次实现 Gate 和返工记录，等待实际部署验收 |

当前没有 `STABLE` 版本。
