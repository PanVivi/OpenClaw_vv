# 魚玄機版本状态

| 版本 | 状态 | 可否作为正式部署源 | 说明 |
| --- | --- | --- | --- |
| v0.01 | `REJECTED` | 否 | 授权、状态、取消和权限回收不完整 |
| v0.02 | `REJECTED` | 否 | 缺少持久化、硬白名单、依赖、基线和脱敏 |
| v0.03 | `REJECTED` | 否 | 运行边界完整，但缺少跨角色任务所有者、Assignment Generation、接收确认和旧代次失效 |
| v0.04 | `CANDIDATE` | 暂否 | 已补齐统一交接契约，等待实际部署验收 |

当前没有 `STABLE` 版本。
