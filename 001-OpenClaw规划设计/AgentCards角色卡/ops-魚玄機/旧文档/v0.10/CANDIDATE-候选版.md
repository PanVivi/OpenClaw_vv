# CANDIDATE 候选版

当前根目录为 v0.10 候选版。基础部署先验收五文件、只读调查、当前会话 Risk 记录、Review 执行拒绝、错误下一跳/重复/材料变化拒绝、分级宿主执行、最小权限、工程流、熔断和回滚；硬 Gate、持久化、精细 A2A 路由与历史授权、技术子 Agent 后续独立验收。

本版完整继承 v0.09，并按既有工程总管职责开放 workspace 文件工具与 `mode=auto` 的 NAS Gateway `exec/process`。生产副作用仍受处理权、正式授权、一次性 Risk、自动执行审查、备份、回滚与 Test 约束；不开放任意 Gateway、Cron、跨会话历史、技术子 Agent 或任意外发消息。
