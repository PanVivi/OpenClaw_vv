# CANDIDATE 候选版

当前根目录为 v0.09 候选版。基础部署先验收五文件、只读调查、当前会话 Risk 记录、Review 执行拒绝、错误下一跳/重复/材料变化拒绝、最小权限、工程流、熔断和回滚；硬 Gate、持久化、精细 A2A 路由与历史授权、技术子 Agent 后续独立验收。

本版完整继承 v0.08，只增加 `ops_telegram_admin` 的固定目标、凭据处理、备份、绑定、校验、探测和回滚规则；不开放通用宿主执行。
