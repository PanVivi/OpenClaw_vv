# 001 OpenClaw架设部署｜RoleCardAudit 角色卡复审｜v0.03

## 一、复审范围

- 基线：`agent/record-housekeeper-deployment-status`，提交 `90cb37404f575a39d97230f3342e8c2afc597b24`
- 本地审核分支：`codex/audit-role-cards`，与远端基线 ahead/behind 为 `0/0`，本轮修复尚未提交
- 对象：八套当前角色包、共同协议、文档规则、当前权威设计/流程/部署/路线/进度、历史归档和未提交差异
- 限制：不增加 Agent、服务、记忆、A2A、自动化或技术子 Agent 范围；不改变已确认人物属性

## 二、本轮发现的遗漏

1. 当前来源索引未列出最新 `DocumentRules v1.04` 和共同协议，接手者可能跳过稳定基线与部署状态规则。
2. 八张 `DeploymentStatus部署状态.md` 没有逐项记录模型、Bot/account/binding、工具、A2A、会话、记忆、自动化、下一步、证据来源和核验时间，不满足 v1.04 的最低交接字段。
3. 賈南風当前 `USER.md` 将 v1.02 的数条用户偏好合并改写，语义虽保留，但不符合“稳定基线不得概括替代”的逐条继承要求。

## 三、已直接修正

- 新建 `SourceIndex v0.06`，补入 `DocumentRules v1.04`、共同协议和本次审核入口。
- 八张部署状态只补充真实的“待核验”字段、下一步和证据时间；没有推测 NAS 配置或把设计写成已部署。
- 賈南風 `USER.md` 恢复 v1.02 原始称呼、工作偏好和真实性条款，再把已确认的新生活路由独立列为补充。
- 新建 `CurrentProgress v0.07`；未改角色版本号，因为当前候选尚未形成固定提交或发布。

## 四、稳定基线与属性核对

- 賈南風 v1.02 的 `IDENTITY.md`、`SOUL.md`、`USER.md` 非空原句已全部在 v1.08 中逐句保留。
- `AGENTS.md`、`TOOLS.md`、`PERMISSIONS.md` 的差异只涉及已确认生活路由、life 自动化唯一所有权、companion 历史最小化和基础/增强分层，并已在 v1.08 继承说明中明确记录。
- 夏姬只替换 reviewer 展示人格名；固定 Agent ID `reviewer`、目录 `reviewer-合并审查` 和 Review/Risk/Test 职责没有迁移。
- 三位 companion 人物属性没有变化。
- 强羞辱性或占有性称呼保持默认可用，原始列举称呼已恢复；停止口令和不得影响事实、权限、风险与任务的既有规则保持不变。

### 賈南風 v1.02 继承与批准矩阵

| 文件 | v1.02 继承 | 当前增量或覆盖 | 批准状态 |
| --- | --- | --- | --- |
| `IDENTITY.md` | 原句全部保留 | 无 | 无需变更 |
| `SOUL.md` | 原句全部保留 | 无 | 无需变更 |
| `USER.md` | 原句全部保留 | 简单生活直接回答、设置型任务转 life、最小完整八 Agent 目标 | 已确认设计；本轮只恢复原句并独立追加 |
| `AGENTS.md` | 未冲突原句保留 | 生活路由、life 自动化唯一所有权、companion 历史最小化、基础/增强分层 | 已确认设计 |
| `TOOLS.md` | 未冲突原句保留 | life 工具所有权、历史默认关闭、技术子 Agent 后置 | 已确认设计 |
| `PERMISSIONS.md` | 未冲突原句保留 | 生活自动化撤出、历史最小化、会话/子 Agent 增强后置 | 已确认设计 |

上述覆盖不扩大 housekeeper 权限；被覆盖的 v1.02 原规则仍保存在 `旧文档/v1.02/`，当前文件的“版本继承要求”明确列出覆盖类别。

## 五、部署判断

- 角色设计与权限边界：通过。
- 文档和交接完整性：本轮遗漏修正后通过。
- 固定部署制品：待提交；当前远端来源分支不包含本地修复。
- NAS 实际上线：待只读盘点、部署和运行验收。
- `STABLE`：除最后已知 housekeeper v1.02 外，其余当前候选均不可提前标记。

因此，当前审核树可以进入固定提交与 Phase 1 受控部署；不能声称已经部署。

## 六、复审验证

- 八套角色包必需文件齐全，Agent ID、人物名和当前版本入口一致。
- 八张 `AGENTS.md` 均内嵌共同协议 v0.02 执行摘要。
- 八张部署状态均覆盖 v1.04 要求的运行维度、下一步、证据与核验时间。
- 当前权威入口和角色卡相对链接无断链。
- 新建的 64 个旧候选归档文件与来源提交对应 blob 一致。
- `.env.example` 仅保留八个空 Bot token 变量，无实际 secret。
- 当前差异通过 `git diff --check`；本轮未连接 NAS。
