这是生产验收，也是少主明确授权的真实生活资料写入。

请使用你当前实际可用的 `life_files` 工具，在少主专属资料区：

1. 创建子目录 `备忘录`；
2. 排他创建文件 `备忘录/Vivi.md`；
3. 文件内容必须准确为以下两段：

这是少主 Vivi 的备忘录。

继续完成OpenClaw 调查任务。该任务之前是进行到一半，尚未完成。。时间上限设定为 3 个小时。

4. 创建后必须再使用 `life_files get` 回读同一文件，核对两段文字。

不得使用 shell，不得访问专属资料区之外的文件，不得启动或恢复调查任务。只有真实创建和回读都成功后才报告成功；否则明确报告失败和真实错误。
