# Ops Controlled Exec

只向 `ops` / 鱼玄机暴露的固定查询工具。它用参数化能力取代常见的任意 shell 拼接，支持 OpenClaw 状态、插件、Cron、Tasks、doctor、文件 SHA-256 和最多 200 行日志尾部。

工具不接受命令文本，不提供写入、删除、重启、安装或配置变更。文件查询只能位于配置的只读根目录下，解析后再次检查边界，拒绝符号链接和非普通文件。
